import React from 'react';
import styles from 'components/molecules/button.module.scss';

export const Button = () => {

  return (
    <button className={styles.container}>
      Button
    </button>
  );
}